{
  "$description": "Color palette for the 'adminlte-classic' template (Classic Admin). Values obtained by observing the public visual identity of the AdminLTE project (ColorlibHQ) — see ATTRIBUTION.md. This is an original document, not an AdminLTE file.",
  "color": {
    "brand": {
      "primary": { "$value": "#0d6efd", "$type": "color", "$description": "Primary action color: main buttons, links, active navigation items, focus rings on interactive elements." },
      "secondary": { "$value": "#6c757d", "$type": "color", "$description": "Secondary/neutral action color: secondary buttons, medium-emphasis supporting text." }
    },
    "status": {
      "success": { "$value": "#198754", "$type": "color", "$description": "Confirmation, completed state, positive values." },
      "danger": { "$value": "#dc3545", "$type": "color", "$description": "Error, destructive action, critical/negative values." },
      "warning": { "$value": "#ffc107", "$type": "color", "$description": "Alert, state pending attention." },
      "info": { "$value": "#0dcaf0", "$type": "color", "$description": "Neutral information, hints, informational badges." }
    },
    "neutral": {
      "light": { "$value": "#f8f9fa", "$type": "color", "$description": "Light neutral: subtle backgrounds, table row hover, section dividers." },
      "dark": { "$value": "#212529", "$type": "color", "$description": "Dark neutral: high-contrast text, background of dark chrome elements (e.g. sidebar)." }
    },
    "surface": {
      "canvas": { "$value": "#ffffff", "$type": "color", "$description": "Default background of content surfaces: cards, tables, panels." },
      "muted": { "$value": "#f4f6f9", "$type": "color", "$description": "Background of the content area behind cards/panels — a light blue-gray that separates the card surface from the rest of the page." },
      "border": { "$value": "#dee2e6", "$type": "color", "$description": "Default color for borders and dividers between surface elements." }
    },
    "text": {
      "primary": { "$value": "#212529", "$type": "color", "$description": "Default body text, on light backgrounds." },
      "secondary": { "$value": "#6c757d", "$type": "color", "$description": "Supporting text, captions, metadata, placeholders." },
      "inverse": { "$value": "#ffffff", "$type": "color", "$description": "Text on dark backgrounds or on top of the primary color." },
      "link": { "$value": "#0d6efd", "$type": "color", "$description": "Default color for text links, same as the primary color." },
      "on-accent": { "$value": "#ffffff", "$type": "color", "$description": "Text and icons placed directly on top of a saturated accent fill. Use ONLY over accents dark enough to carry white text at 4.5:1 or better: brand.primary (4.50:1), brand.secondary (4.69:1), status.success (4.53:1), status.danger (4.53:1), neutral.dark (15.43:1). Over any other fill use text.on-accent-dark." },
      "on-accent-dark": { "$value": "#212529", "$type": "color", "$description": "Text and icons placed directly on top of a LIGHT accent fill, where text.on-accent would fail WCAG AA. Required over status.warning (white is only 1.63:1; this token is 9.46:1), status.info (white is 1.96:1; this token is 7.88:1) and neutral.light (14.63:1). Every filled-surface component (Stat Callout, Badge, Alert, Button) must pick between this token and text.on-accent based on the accent it renders on — never default to white." }
    },
    "chrome": {
      "sidebar-bg": { "$value": "#212529", "$type": "color", "$description": "Background of the dark side navigation — a signature trait of the classic 'admin dashboard' look." },
      "sidebar-text": { "$value": "#c2c7d0", "$type": "color", "$description": "Default text color of side navigation items, at rest, on the dark background." },
      "sidebar-text-active": { "$value": "#ffffff", "$type": "color", "$description": "Text color of the side navigation item on hover, focus, or active state." },
      "sidebar-item-hover-bg": { "$value": "rgba(255, 255, 255, 0.1)", "$type": "color", "$description": "Background of a side navigation item on hover — a translucent white overlay on top of the dark background, the same pattern used for the active state." },
      "sidebar-item-active-bg": { "$value": "rgba(255, 255, 255, 0.1)", "$type": "color", "$description": "Background of the selected/active side navigation item." },
      "topbar-bg": { "$value": "#ffffff", "$type": "color", "$description": "Background of the top bar (topbar) — light, contrasting with the dark sidebar." }
    },
    "chart": {
      "series-1": { "$value": "#0d6efd", "$type": "color", "$description": "First data series in a chart. Matches brand.primary so the dominant series reads as the page's primary accent." },
      "series-2": { "$value": "#20c997", "$type": "color", "$description": "Second data series — a teal that separates from series-1 in hue and lightness. Color alone must never be the only way series are told apart: a chart using two or more series requires a legend or a per-series shape/pattern, since blue and teal are hard to separate under deuteranopia." },
      "series-3": { "$value": "#fd7e14", "$type": "color", "$description": "Third data series — a warm orange, reserved for charts that genuinely need a third line. Prefer splitting into two charts over adding a fourth series." },
      "grid": { "$value": "#dee2e6", "$type": "color", "$description": "Axis lines, gridlines and tick marks inside a chart canvas. Same value as surface.border so chart chrome recedes to the same weight as the surrounding surface dividers." },
      "sparkline": { "$value": "#6c757d", "$type": "color", "$description": "Line and fill color of a compact sparkline that carries no axes or legend. Deliberately muted (matches text.secondary) so a strip of sparklines reads as supporting detail rather than competing with the primary chart on the same page." }
    },
    "overlay": {
      "backdrop": { "$value": "rgba(33, 37, 41, 0.5)", "$type": "color", "$description": "Dimming layer painted behind a modal, offcanvas panel, or off-canvas sidebar. Built from neutral.dark at 50% opacity so page content stays faintly readable while clearly demoted; the overlay surface itself sits on surface.canvas above it." },
      "accent-shade": { "$value": "rgba(33, 37, 41, 0.15)", "$type": "color", "$description": "Darkening wash laid over a surface that is already filled with an accent colour, to separate a sub-area from the fill without introducing a second hue — e.g. a Stat Callout's footer strip, or the hover state of a control sitting on a filled surface. Works over both dark and light accents; never used on an unfilled (surface.canvas) background, where surface.border or neutral.light applies instead." },
      "accent-glyph": { "$value": "rgba(255, 255, 255, 0.3)", "$type": "color", "$description": "Colour of an oversized DECORATIVE glyph sitting on a dark accent fill (brand.primary, brand.secondary, status.success, status.danger). Deliberately low-contrast: this is background texture, not content, and must be aria-hidden. Never use it for a glyph that carries meaning — that one takes text.on-accent." },
      "accent-glyph-dark": { "$value": "rgba(33, 37, 41, 0.2)", "$type": "color", "$description": "The accent-glyph counterpart for LIGHT accent fills (status.warning, status.info, neutral.light), where a white glyph would be invisible. Same rule: decorative only, aria-hidden, never load-bearing." }
    }
  }
}
